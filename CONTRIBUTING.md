# 참여 방법

## 새 세션 만들기

`templates/session/`을 `sessions/번호-유형/`으로 복사합니다. Zero는 `01-theory`, `02-implementation`처럼 원리 탐구와 구현을 교대하고, Frontier는 `01-exploration`, `02-exploration`처럼 번호를 올립니다. 이미 만든 세션은 덮어쓰지 않습니다.

`개인명/`을 실제 이름으로 바꾸고 필요하면 개인 폴더를 더 만듭니다. 개인 기록 형식은 자유이고 템플릿은 참고용입니다. 세션 README에 주제·시작일·공유 예정일·상태·개인 기록 링크를 적고, 저장소 README의 세션 목록에도 추가합니다.

한 세션은 약 2주간의 학습 단위입니다. 날짜 폴더 하나에 시작 회의와 결과 발표를 혼합하지 않고, 세션 안의 `meetings/`에서 실제 미팅 날짜를 구분합니다.

## 미팅 기록

- `templates/meeting.md`를 해당 세션의 `meetings/YYYYMMDD-종류.md`로 복사합니다.
- 종류는 `kickoff`, `review`, `review-and-kickoff`, `interim` 등으로 정합니다.
- 세션 README와 `meetings/README.md`에 날짜·종류·주요 내용·회의록 링크를 남깁니다.
- 한 미팅에서 이전 세션 회고와 다음 세션 킥오프를 함께 했다면 **이전 세션에 한 번만 보관**하고, 다음 세션의 시작 회의록으로 같은 문서를 연결합니다.
- 예: Zero 01의 `meetings/20260921-review-and-kickoff.md`를 Zero 02 README에서 `../01-theory/meetings/20260921-review-and-kickoff.md`로 연결합니다. 실제 파일을 작성한 뒤 링크합니다.
- 예정 미팅은 목록에 예정 상태로만 표시합니다. 아직 하지 않은 회의의 결과나 결론은 작성하지 않습니다.
- 전체 운영을 논의한 첫 미팅은 첫 세션에 보관하고, 다른 저장소에서는 GitHub 원본 링크로 연결할 수 있습니다.

## 개인 작업

처음에는 공용 `main` 브랜치에 직접 커밋하고 push합니다. 아래 예시의 날짜와 이름은 실제 경로로 바꾸세요. 저장소 폴더 안에서 실행합니다.

```bash
# 작업 시작 전: 이전 작업을 커밋한 상태에서 실행
 git pull --rebase

# 자기 폴더에서 기록·실험 후 해당 경로만 추가
 git add sessions/번호-유형/개인명/
 git diff --cached
 git commit -m "docs: 회차 개인 탐구 기록"

# 다른 사람의 변경을 반영하고 업로드
 git pull --rebase
 git push
```

- 자기 폴더를 중심으로 수정합니다. 회의록과 회차 안내는 담당자에게 반영을 요청합니다.
- 다른 사람이 먼저 push했다면 파일이 달라도 push가 거절될 수 있습니다. `git pull --rebase` 후 다시 push합니다.
- 실제 충돌이 나면 해당 작성자와 내용을 확인하고 충돌 표시를 정리한 뒤, 수정 파일을 `git add`하고 `git rebase --continue`합니다. 해결 방향이 불명확하면 `git rebase --abort`로 이번 rebase를 취소하고 함께 확인합니다.
- 공용 브랜치에는 강제 push(`--force`, `--force-with-lease`)를 하지 않습니다.
- 다른 사람의 폴더나 공통 운영 원칙을 바꿀 때는 먼저 상의합니다. 검토가 필요한 변경은 선택적으로 PR을 사용합니다.
- 인증 정보와 실제 비밀값은 커밋하지 않습니다. 실험에 필요한 설정 예시는 `.env.example`처럼 값이 없는 예제로 남깁니다.

## 모임 전후

1. 시작: 학습 주제와 범위를 정하고 세션 README 및 킥오프 회의록을 연결합니다.
2. 학습 중: 각자 개인 폴더에 기록과 코드를 올립니다. 중간 미팅이 있으면 날짜별 회의록을 추가합니다.
3. 공유 모임: 이전 세션의 결과·회고와 다음 세션의 주제·범위를 정리합니다. 기록 담당자는 돌아가며 맡습니다.
4. 공유 후: 미팅 목록과 세션 상태를 갱신하고, 다음 세션 README에 킥오프 원본 회의록을 연결합니다. 탐구 연장 시에는 예정일과 이유를 기록합니다.
